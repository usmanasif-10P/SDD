[project]
name = "todo-management-app"
version = "0.1.0"
description = "FastAPI + PostgreSQL todo management service"
requires-python = ">=3.11"
readme = "README.md"

[tool.pytest.ini_options]
asyncio_mode = "auto"
testpaths = ["tests"]
pythonpath = ["."]
